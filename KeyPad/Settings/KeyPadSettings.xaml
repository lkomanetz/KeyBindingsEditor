<UserControl x:Class="KeyPad.Settings.KeyPadSettings"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:local="clr-namespace:KeyPad.Settings"
			 xmlns:vm="clr-namespace:KeyPad.Settings.ViewModels"
			 mc:Ignorable="d"
			 d:DataContext="{d:DesignInstance {x:Type vm:KeyPadSettingsDesignViewModel}, IsDesignTimeCreatable=True}"
			 d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<TreeView Grid.Column="0"
				  ItemsSource="{Binding SettingsMenu}">
			<TreeView.ItemTemplate>
				<HierarchicalDataTemplate DataType="{x:Type local:TreeMenuItem}"
										  ItemsSource="{Binding Children}">
					<TextBlock>
						<Hyperlink TextDecorations="None"
								   Cursor="Arrow"
								   Command="{Binding Action}"
								   Foreground="Black">
							<TextBlock Text="{Binding Title}"/>
						</Hyperlink>
					</TextBlock>
				</HierarchicalDataTemplate>
			</TreeView.ItemTemplate>
		</TreeView>
		<ContentPresenter Grid.Column="1"
						  DataContext="{Binding CurrentViewModel}"/>
	</Grid>
</UserControl>
